{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "title": "Speedscale BYOC OTel Collector → S3 chart values",
  "type": "object",
  "additionalProperties": false,
  "required": ["s3"],
  "properties": {
    "s3": {
      "type": "object",
      "additionalProperties": false,
      "required": ["bucket", "region"],
      "properties": {
        "bucket": {
          "type": "string",
          "minLength": 1,
          "description": "S3 bucket name (required) — must exist before installing"
        },
        "region": {
          "type": "string",
          "minLength": 1,
          "description": "AWS region where the bucket lives (e.g. us-east-1)"
        },
        "prefix": {
          "type": "string",
          "minLength": 1,
          "description": "Base S3 prefix for archived OTLP JSON objects"
        },
        "endpoint": {
          "type": "string",
          "description": "Custom S3 endpoint for non-AWS providers (e.g. https://nyc3.digitaloceanspaces.com). Leave empty for AWS S3."
        },
        "forcePathStyle": {
          "type": "boolean",
          "description": "Force path-style URLs (bucket in path, not subdomain). Required for most S3-compatible providers."
        },
        "credentialsSecret": {
          "type": "string",
          "description": "K8s Secret name with accessKeyId + secretAccessKey. Set to empty string for IRSA."
        }
      }
    },
    "irsa": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Enable IRSA (EKS only). When true, roleArn is required."
        },
        "roleArn": {
          "type": "string",
          "pattern": "^(arn:aws:iam::[0-9]{12}:role/.+)?$",
          "description": "IAM role ARN for IRSA (e.g. arn:aws:iam::123456789012:role/my-role)"
        },
        "serviceAccountName": {
          "type": "string",
          "minLength": 1,
          "description": "Name of the ServiceAccount created when IRSA is enabled"
        }
      },
      "if": {
        "properties": { "enabled": { "const": true } }
      },
      "then": {
        "required": ["roleArn"],
        "properties": {
          "roleArn": {
            "minLength": 1,
            "description": "Required when irsa.enabled=true"
          }
        }
      }
    },
    "image": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "otelCollector": {
          "type": "string",
          "minLength": 1,
          "description": "Must be opentelemetry-collector-contrib (awss3 is contrib-only)"
        },
        "awsCli": {
          "type": "string",
          "minLength": 1,
          "description": "AWS CLI image used by the optional layout manifest upload job"
        }
      }
    },
    "layout": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Enable workload-aware S3 prefixes and publish _speedscale/byoc-layout.json"
        },
        "partition": {
          "type": "string",
          "enum": ["minute"],
          "description": "Time partition granularity used by the awss3 exporter"
        },
        "workloads": {
          "type": "array",
          "description": "Known workloads to write under namespace/app/time prefixes",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["namespace", "appLabel"],
            "properties": {
              "namespace": {
                "type": "string",
                "minLength": 1
              },
              "appLabel": {
                "type": "string",
                "minLength": 1
              }
            }
          }
        }
      }
    }
  }
}
